# Resume（本機專案）

個人履歷網頁。含真實個資，**僅存本機與 GitHub private 倉庫，永不設為 public、不部署、不傳 MEDIA**。

## 檔案
- `resume.md` — 來源（Big5）
- `resume.html` — 成果（self-contained，可直接瀏覽 / 列印成 PDF）

## 隱私
- remote 僅 `HaleyChui/Resume`（**private**）；若誤設 public 立即改回。
- 不引入外部資源（無 CDN / 字型 / 分析）。
- 上線前請自行評估 email/電話防爬與 CSP（本專案不含部署）。
